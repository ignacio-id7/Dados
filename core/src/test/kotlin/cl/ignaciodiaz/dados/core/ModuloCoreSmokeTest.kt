package cl.ignaciodiaz.dados.core

import org.junit.Assert.assertTrue
import org.junit.Test

// Prueba de humo: confirma que el módulo :core compila y que los tests corren.
// Sin lógica de juego todavía; eso se diseña e implementa aparte.
class ModuloCoreSmokeTest {

    @Test
    fun `el modulo core compila y ejecuta tests`() {
        assertTrue(true)
    }
}
